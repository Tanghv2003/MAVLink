#include <stdio.h>
#include <stdint.h>
#include "system.h"
#include "led.h"
#include "tx.h"
#include "rx.h"
void delay(int v){
	while(v--);
}
int main(){
	SetLed(10);
	char p[] = "Hello\n";
	while(1){
		send_statusText(p);
		delay(1000000);
	}
	return 0;
}
