#include <stdio.h>
#include "tx.h"
#include "rx.h"
int main()
{
	printf("=======================================\n");
	Frame f;
    while(1){
    	//sendText();
    	//readFrame(&f);
    	//send_statusText("xong 1 frame\n");
    	sendText();
    }
}
