#include <stdio.h>
#include <stdint.h>
#include "system.h"
#include "led.h"
#include "tx.h"
#include "rx.h"

void delay(int v){
	while(v--);
}

void test_cpu(){
	while(1){
		printf("abcd\n");
		delay(1000000);
	}
}
int main(){
	SetLed(31);
	test_cpu();
	//decode();
	return 0;
}
